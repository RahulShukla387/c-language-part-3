#include<stdio.h>
int main (){
    int a = 45;
    int b = 60;
    // this is called ternary operator basically shortcut of if else statement
    a<b?printf("a is less than b"):printf("a is greater than b");
    return 0;
}