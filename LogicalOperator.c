#include<stdio.h>
int main(){
    // and operator (&&) ;or operator (||) ; bitwise operator(!=)
    int a =1;
    int b = 0;
    printf("the value of result is %d \n",a&&b );
    printf("the value of result is %d \n",a||b );
    printf("the value of result is %d \n",!b);
    return 0;

}